"""Phase 5.5 response-contract helpers.

These modules build the product-safe response surface (insight ids,
typed evidence, anchors, capability reason codes, rhyme evidence tags)
from the domain-layer outputs of Phase 5. They sit between the domain
and the schema layer so the service stays a thin orchestrator.
"""
